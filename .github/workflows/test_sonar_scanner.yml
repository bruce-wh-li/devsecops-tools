name: sonar-maven-scan
on:
  workflow_dispatch:
  push:
jobs:
  sonar-scan-mvn:
    uses: bruce-wh-li/devsecops/.github/workflows/sonar-scanner-mvn.yaml@main
    with:
      WORKDIR: ./tekton/demo/maven-test
### Sonar Cloud Setting  ### 
      PROJECT_KEY: maven-test-bruce
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
