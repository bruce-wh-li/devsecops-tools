#name: sonar-repo-scan
#on:
#  workflow_dispatch:
#  push:
#jobs:
#  sonar-repo-scan:
#    uses: bruce-wh-li/security-pipeline-templates/.github/workflows/sonar-scanner.yaml@main
#    with:
#      ORG: ci-testing
#      PROJECT_KEY: secruity_pipeline_templates
#      URL: https://sonarqube-59e31e-tools.apps.klab.devops.gov.bc.ca/projects
#     # URL: https://rocketchat-bruce-59e31e-dev.apps.klab.devops.gov.bc.ca/
#    secrets:
#      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#      
#      
      on:
  # Trigger analysis when pushing in master or pull requests, and when creating
  # a pull request. 
  push:
    branches:
      - master
  pull_request:
      types: [opened, synchronize, reopened]
  workflow_:
#
name: Main Workflow
jobs:
  sonarqube:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        # Disabling shallow clone is recommended for improving relevancy of reporting
        fetch-depth: 0
    - name: SonarQube Scan
      uses: sonarsource/sonarqube-scan-action@master
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
